# Cantonese Speech to Text - Environment Variables

# API Configuration
# Your Fal.ai API key - get from https://fal.ai/dashboard
# Format: your-key-id:your-secret-key
FAL_API_KEY=your-fal-ai-api-key-here

# Application Configuration
# The URL where your app is deployed (for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Default settings (optional)
# Default language for transcription
NEXT_PUBLIC_DEFAULT_LANGUAGE=yue

# Feature flags (optional)
# Enable/disable features by default
NEXT_PUBLIC_DEFAULT_SPEAKER_ID=false
NEXT_PUBLIC_DEFAULT_CHINESE_CONVERSION=true

# Analytics (optional)
# Add your analytics tracking ID
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_MIXPANEL_TOKEN=

# Performance (optional)
# Maximum file size in MB
NEXT_PUBLIC_MAX_FILE_SIZE_MB=50

# Development only
# Set to development for local testing
NODE_ENV=development

# Security (production only)
# Session secret for production (generate a random string)
SESSION_SECRET=your-random-session-secret-here 